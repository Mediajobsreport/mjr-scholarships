{
  "name": "mjr-scholarships-fellowships",
  "version": "1.0.0",
  "private": true,
  "description": "Searchable scholarship and fellowship directory for Media Jobs Report",
  "scripts": {
    "build": "node scripts/build-scholarships.js",
    "check": "node scripts/build-scholarships.js --check",
    "check:links": "node scripts/check-scholarship-links.js",
    "check:links:offline": "node scripts/check-scholarship-links.js --offline",
    "serve": "npx --yes serve public"
  },
  "engines": {
    "node": ">=20"
  }
}
